import localFont from "next/font/local";
import { Inter } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });

export const cloisterBlack = localFont({
  src: "../../public/font/CloisterBlack.ttf",
});
